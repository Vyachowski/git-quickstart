h2.display-5.mb-3 Configuration
+guideChunk(
  1,
  'Set up your name',
  'git config --global user.name "your name"',
  '',
  '',
  )
+guideChunk(
  2,
  'Set up your email',
  'git config --global user.email "your email"',
  '',
  'Important: Your email should be equal to your email on GitHub (if you use it)',
  )
h2.display-5.mb-3 Initialization
+guideChunk(
  3,
  'Start a new repo',
  'git init',
  'rm -rf .git',
  'To remove git from the repository you can just delete the .git folder from your file manager',
  )
h2.display-5.mb-3 Clone repo
+guideChunk(
  4,
  'Clone an existing local repo',
  'git clone path/to/repo',
  '',
  'To undo this action just delete all files and subfolder except .git',
  )
+guideChunk(
  5,
  'Clone an existing remote GitHub repo',
  'git clone https://github.com/user/repo.git',
  '',
  'To undo this action just delete all files and subfolder except .git',
  )
h2.display-5.mb-3 Start tracking files
+guideChunk(
  6,
  'Add',
  'git add <filename>',
  'git restore --staged <filename>',
  'Or replace a filename with . to add all files',
  )
